# 全市场异动雷达智能体 · 项目说明

> 阅读顺序就按快报的顺序：先 30 秒版结论，再展开细节。读完这页就能上手网页与命令行。

---

## [1] 30 秒版

全市场异动雷达是一款数字资产盯盘工具：一次扫描上千个交易对，
按 **0-100 综合异动强度**排序，输出一份「头条异动 + 异动榜单 + 口径与诚实说明」的快报。

- **网页版**：星空雷达页，自动刷新、可筛选、点币种出简报；
- **命令行版**（本次新增）：同一套评分引擎，四路数据探头（含币安官方三路），
  `--json` 可被其他程序直接消费；
- **数据只读公开接口**，不要 API 密钥，不接账户，不下单；
- **评分公式完全公开**（见 [6]），每一条结论都能逐项复算。

## [2] 它解决什么问题

盯盘最累的不是看行情，是**在全市场里找「哪里出事了」**。
这个作品把这件事变成一份快报：

1. 全市场横截面扫描 —— 不锁定币种，谁异动谁上榜；
2. 综合强度排序 —— 涨跌剧烈 + 成交活跃的组合排前面，单看涨跌幅容易错过「放量异动」；
3. 先给结论再给口径 —— 头条一句话说清「谁、动了多少、多活跃」，
   分数怎么来的、方向是什么口径，全部写在快报尾部，不藏在文档里。

## [3] 网页版功能

- **自动扫描**：定时自动刷新，展示下次刷新倒计时，也可点「立即扫描」手动触发；
- **多维筛选**：按平台、涨跌方向、币种名称筛选榜单；
- **异动简报**：点击榜单任意币种，展开聚合简报 —— 现价、24h 涨跌幅、
  成交量/成交额表现、主动买卖方向、综合异动等级、一段文字说明；
- **数据降级如实声明**：公开接口可能受网络环境、地区策略、维护或限频影响，
  取不到数时自动切换**演示数据**保证页面可体验，切换时页面会明确声明
  当前是演示数据 —— 不冒充实时（命令行版没有降级，取不到数就报错退出）。

## [4] 命令行版用法

```bash
node agent.mjs                                # 默认：多平台实时对照（与网页版同源）
node agent.mjs --live                         # 币安官方公开行情，现货全市场快照
node agent.mjs --skill                        # 官方技能 binance + 官方 CLI
node agent.mjs --official --max-coins 60      # 官方开源数据仓库（T+1，含持仓量维度）
node agent.mjs "看看官方归档里的历史异动"        # 自然语言选通道（识别不出就静默走默认）

node agent.mjs --live --top 20                # 榜单拉长到 20 条
node agent.mjs --live --direction fall        # 只看急跌
node agent.mjs --live --min-score 70          # 只看 70 分以上
node agent.mjs --official --coins BTC,ETH     # 官方通道只扫指定币
node agent.mjs --live --json                  # 纯 JSON（进度与说明走 stderr）
```

四路探头实测（2026-09-14，数字随行情与网络浮动）：

| 通道 | 监测市场 | 耗时 | 通道标识 |
|---|---|---|---|
| 默认（多平台） | 1365 | 0.9 秒 | `via=multi-platform` |
| `--live` | 681 | 0.9 秒 | `via=official-rest` |
| `--skill` | 681 | 1.1 秒 | `via=binance-cli` |
| `--official` | 40 币拼齐 33 | 数秒 | `via=official-archive`（锚 2026-09-13） |

## [5] 数据从哪来

| 探头 | 数据源 | 说明 |
|---|---|---|
| 默认 | Gate.io + Hyperliquid 公开行情 | 与网页版同源，跨平台交叉观察 |
| `--live` | 币安官方公开行情接口 | `ticker/24hr` 不带 symbol 一次拿全现货，免密钥 |
| `--skill` | 官方技能 binance 驱动的官方 CLI | 官方 CLI 未安装时如实回退并标注 |
| `--official` | 币安官方开源数据仓库 | 合约日 K 线 + 合约指标（5 分钟粒度持仓量美元值），T+1 归档 |

设计原则：**探头可以坏，坏要说**。默认通道某平台取不到数如实报错；
`--skill` 回退会在 JSON（`requestedSource` 字段）与诚实说明里标注；
官方归档缺文件的币种进 `missingPreview` 列出，不补齐、不猜数。

## [6] 评分怎么打（公式公开）

引擎是 `app.js` 的 `scoreMarkets`（网页与命令行同一段代码，源码逐字节提取复用）：

1. **涨幅分** = min(|24h 涨跌幅| × 5.3, 82)；
2. **活跃度分** = 该币成交额在本批样本里的百分位（0-100）；
3. **方向加成** = |涨跌幅| ≥ 2% 加 8 分，否则加 2 分；
4. **综合分** = min(99, round(涨幅分 × 0.62 + 活跃度分 × 0.3 + 方向加成))。

等级：≥80 高关注（显著异动），≥60 计入「显著异动」统计；急涨/急跌 = ±5%。
**方向口径**：涨 = 买盘方向、跌 = 卖盘方向（价格方向口径，
公开快照无法确认真实主动成交流向 —— 网页简报与命令行快报都如此声明）。

## [7] 别过度解读的边界

- 快报回答「哪里在异动」，**不预测**「接下来涨还是跌」；
- 活跃度分位是**样本内相对值**，跨通道不可直接比较；
- `--official` 是 T+1 口径，价格截至上一交易日收盘；个别新币涨跌基准用当日开盘
  （JSON `baseline` 字段标注）；
- 样本过小（`--coins` 指定少数币）时，评分仅在样本内相对比较，快报会提示参考意义有限；
- 评分用于排序观察，**不构成投资建议**。

## [8] 项目文件与运行

```text
yidong-leida-agent/
├─ index.html / styles.css / app.js   # 网页版（星空雷达 + 评分引擎）
├─ agent.mjs                          # 命令行入口（四通道 + 自然语言 + 快报输出）
├─ AGENT.md                           # 自查卡（快报样张 + 三步自查 + 技能取舍）
├─ skills/quanshichang-yidong/SKILL.md  # 官方技能定义
├─ netlify.toml                       # Netlify 配置（纯静态，发布目录 .）
└─ README.md / 项目说明.md
```

运行：`node agent.mjs`（命令行，需 Node.js 18+，零第三方依赖）；
`python -m http.server 8787`（网页版本地服务）；或直接双击 `index.html`。

## [9] 免责声明

本项目仅用于数字资产市场数据观察、异动展示与数据分析演示，不构成任何投资、
交易、收益或风险承诺。数字资产价格波动剧烈，请独立判断并自行承担风险。
