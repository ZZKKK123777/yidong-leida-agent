# AGENT.md · 全市场异动雷达 · 当班快报说明书

> 一句话：这是网页版的**命令行分身** —— 数据探头可以换四路（含币安官方三路），
> 出分引擎是同一段代码。这份文档也按快报的方式写：**先看结论，再对口径，最后教你自查。**

---

## 先看结论：一份真实的当班快报

下面是 `node agent.mjs --live --top 5` 在 **2026-09-14 实测**的原样输出
（行情在动，你跑出来的数字一定不同；但版式、字段、通道标识应当一致）：

```text
  ──────────────────────────────────────────────────────────────────
   全市场异动雷达 · 当班快报     2026-09-14 19:28:38 UTC
  ──────────────────────────────────────────────────────────────────
   数据通道：币安官方公开行情（现货全市场快照）
   通道标识：source=live  via=official-rest
   监测市场 681 个 · 显著异动（≥60 分）75 个
   急涨（≥+5%）49 · 急跌（≤-5%）110 · 买盘方向 42% / 卖盘方向 58%

  ▶ 头条异动：LSK（币安官方公开行情）
      现价 0.4303　24h -54.83%　异动强度 89/100（高关注）
      活跃度分位 99%　卖压偏强（价格方向）
      快报：LSK 当前价格向下偏移，24h 变动 -54.83%。成交活跃度处于样本的 99 分位，归类为显著异动。

  ▶ 异动榜单（前 5）
      排名  币种            涨跌幅      强度  分位  方向  来源
      01    LSK            -54.83%    89   99%  跌    官方公开行情
      02    REZ            -17.44%    88   96%  跌    官方公开行情
      03    VTHO           -16.73%    87   93%  跌    官方公开行情
      04    MINA           -16.97%    86   89%  跌    官方公开行情
      05    ARK            +14.98%    85   94%  涨    官方公开行情

  ▶ 口径与诚实说明
      · 评分与活跃度分位由本项目自研引擎（app.js 的 scoreMarkets，与网页版同一段代码）计算；数据由对应通道提供。
      · direction / "买盘方向" 字段是【价格涨跌方向】口径：涨=买盘方向、跌=卖盘方向。
      · activity 是同一批样本内的成交额横截面分位：跨通道不可直接比较。
      · 综合异动强度是自研评分，用于排序观察，不构成投资建议。
      · 原始返回 3702 个交易对，按 USDT 计价 + 24h 成交额 > $10,000 过滤后 681 个
```

快报每一部分是谁产出的：

| 快报部分 | 谁负责 |
|---|---|
| 抬头（数据通道 / 通道标识 / 监测市场数） | 数据探头如实自报 |
| 头条异动 / 异动榜单 / 异动强度评分 | **自研引擎**（`app.js` 的 `scoreMarkets`，与网页版同一段代码） |
| 口径与诚实说明 | 程序自己声明，不靠文档补 |

---

## 教你自查：三步，每步一条命令

不要求你相信上面任何一句描述。三步自查，看的是程序**自己写的通道标识**——
它写的就是它真走的，不会替你猜。

### 自查 1 · 看它自报家门

```bash
node agent.mjs --live --json
```

看 JSON 开头的三个字段（人类可读输出的抬头里也有同样内容）：

```json
"source": "live",
"via": "official-rest",
"dataSource": "币安官方公开行情（现货全市场快照）"
```

`via`（实际走的路）有四个值：`multi-platform` = 多平台公开行情对照（默认通道）、
`official-rest` = 币安官方公开行情接口、`binance-cli` = 官方 CLI、
`official-archive` = 币安官方开源数据仓库。

### 自查 2 · 看官方 CLI 是不是真的被调用

```bash
node agent.mjs --skill --json
```

**`via` 应当变成 `binance-cli`。** 实测 681 个市场、端到端约 1.1 秒。

如果你自己的机器**没装**官方 CLI，程序会打印官方安装地址、并**如实回退**
到同一份官方公开行情（此时 `via` 是 `official-rest`，JSON 里多出
`requestedSource: "skill"` 字段，诚实说明里多一条回退声明）。
—— 它宁可说自己没调成，也不会假装调过。

### 自查 3 · 看官方归档的日期锚得对不对

```bash
node agent.mjs --official --json
```

看 `archive` 字段（实测样例）：

```json
"archive": { "anchor": "2026-09-13", "prev": "2026-09-12", "missingCount": 7, "missingPreview": ["MATIC", "kPEPE", "RNDR"] }
```

官方开源数据仓库是 **T+1** 归档：今天的文件还没发布，所以 `anchor`
一定是「上一个已收盘交易日」。程序从今天往前探测、命中即用，
缺文件的币种进 `missingCount` / `missingPreview` 如实列出，**不补齐、不猜数**。
实测 40 币种拼齐 33 个，头条 WLD −5.19%（截至 2026-09-13 收盘口径）。

---

## 四路探头怎么选

| 敲什么 | 数据从哪来 | 适合什么时候 |
|---|---|---|
| `node agent.mjs` | 多平台公开行情对照（与网页版同源） | 想和网页版互相印证 |
| `node agent.mjs --live` | 币安官方公开行情，现货全市场快照 | 最快的官方口径（实测 0.9 秒） |
| `node agent.mjs --skill` | 官方技能 binance + 官方 CLI | 要体现官方技能调用链时 |
| `node agent.mjs --official --max-coins 60` | 官方开源数据仓库（合约 K 线 + 持仓量） | 要合约持仓量维度、T+1 口径时 |

自然语言也认：`node agent.mjs "看看官方归档里的历史异动"` 会自动选
`--official` 通道；识别不出就不吭声走默认通道，**绝不拿一句话乱猜**。

常用参数：`--top N`（榜单条数）、`--min-score N`（只看 ≥N 分）、
`--direction rise/fall`（只看急涨/急跌）、`--coins A,B`（官方通道只扫指定币）、
`--json`（纯 JSON，进度走 stderr）。全部用法 `node agent.mjs --help`。

---

## 官方资源 ↔ 快报对应行

| 币安官方资源 | 用在哪一步 | 快报里对应哪一行 | 实测证据 |
|---|---|---|---|
| 官方公开行情接口 **data-api.binance.vision** | `--live`：`api/v3/ticker/24hr` 不带 symbol，一次拿现货全市场 | 「数据通道：币安官方公开行情（现货全市场快照）」 | 实测原始 3702 个交易对 → 过滤后 681 个，0.9 秒 |
| 官方技能 **binance** + 官方 CLI | `--skill`：官方 CLI 的 `request` 子命令取同一份官方公开行情 | 「通道标识：via=binance-cli」 | 实测 681 个市场、1.1 秒，`via` 真实变化 |
| 官方开源数据仓库 **data.binance.vision** | `--official`：合约日 K 线 + 合约指标（5 分钟粒度持仓量美元值）逐币拼横截面 | 「数据通道：……截至 2026-09-13 收盘」+ 榜单 `oiUsd` 维度 | 实测锚定 2026-09-13，40 币拼齐 33 个，缺 7 个如实列出 |
| 多平台公开行情（Gate.io / Hyperliquid） | 默认通道：与网页版同源的交叉对照 | 「via=multi-platform」 | 实测 1365 个市场、0.9 秒 |

**价值边界一句话**：数据层能拿官方的一律拿官方（上面三路币安官方探头）；
评分算法与结论层是自研 —— 官方没有「把全市场横截面按异动强度排成一份
头条+榜单+口径说明的快报」这个产物，这是本作品的价值。

**引擎没被动过的证据**：`app.js` 里没有导出、顶层直接操作页面元素，
命令行跑不了。所以 `agent.mjs` 用**源码级提取**：从 `app.js` 里按函数边界
原样切出 `scoreMarkets` 等 6 个函数（每个碎片都断言「逐字节存在于 app.js」），
再用 `new Function` 装配调用 —— **引擎一行没改**，网页版和命令行出的是同一套分。

---

## 官方 20 个技能的取舍（逐个核过，不是看了看文档）

币安官方技能市场共 20 个技能，分两组。本作品只接了 1 个，理由如下：

| 技能 | 结论 |
|---|---|
| `binance` | ✅ **在用** —— `--skill` 通道经它驱动的官方 CLI 取数 |
| `academy-skill` / `fiat` / `square-post` | ❌ 接口走 `www.binance.com`，实测连接被拒绝（502） |
| `p2p` | ❌ 走 `c2c.binance.com`，不可达 |
| `payment` / `onchain-pay` | ❌ 必须自备 API 密钥并签名；本作品是纯公开数据作品，不用密钥 |
| Web3 组 13 个 | ⚠️ 主力接口全部挂在 `web3.binance.com`，本机网络实测被 SNI 层阻断；其中 `query-token-info` 的 kline 子命令可达，但它给的是链上 K 线，与本作品的中心化交易所横截面口径不匹配，未接入 |

另外，官方各语言 SDK 连接器（connector 类仓库）也没有引入：它们面向
「用 API 密钥交易账户」的场景，本作品只读公开数据，引入会变成死依赖。

> 关于「官方是不是已有异动/信号类能力」：Web3 组确有 `trading-signal` 等信号技能，
> 但接口不可达，且它们输出官方模型的综合判定；本作品是**可逐项复核的规则口径**
> （评分公式公开在 SKILL.md，输入是什么、每一分怎么来的全部可算）。互补，不重复。

---

## 诚实边界（读快报前先读这个）

1. **「买盘方向 / 卖盘方向」是价格方向口径**：涨 = 买盘方向、跌 = 卖盘方向。
   公开行情快照无法确认真实主动成交流向 —— 网页版简报里也是同样声明。
2. **活跃度分位是样本内的相对值**：通道不同、过滤不同，分位就不同，跨通道不可直接比。
3. **`--official` 是 T+1 口径**：价格与涨跌截至上一交易日收盘，快报抬头写明锚定日期；
   涨跌基准以前一交易日收盘价为准，个别新币用当日开盘（JSON `baseline` 字段标注）。
4. **缺文件不补数**：官方归档缺的币种如实列出（实测 7 个），持仓量缺失时 `oiUsd` 留空。
5. **命令行不做演示数据降级**：取不到数就如实报错退出（网页版有演示降级并在页面声明）。
6. **样本过小会明说**：`--coins` 只指定少数币时，分位与评分仅在样本内相对比较，
   快报里会附「参考意义有限」的提示。
7. **不构成投资建议。**

---

## 与已提交参赛说明的关系

- **网页形态**（`index.html` + `app.js`）继续保留，功能与口径都不变；
- **新增加**的是命令行入口 `agent.mjs` 与技能定义 `skills/quanshichang-yidong/SKILL.md`，
  让这个作品可以被技能安装器装载、被其他 Agent 直接调用；
- 两种形态**共用同一段评分引擎**（`scoreMarkets`），结论口径完全一致。
