---
name: quanshichang-yidong
description: |
  全市场异动雷达。对整个市场做横截面扫描：一次拉起上千个交易对的 24 小时涨跌幅、
  成交额与持仓量，识别急涨、急跌与成交活跃度异常，并用自研 0-100 综合异动强度评分
  排序，产出一份「先给结论、再给口径」的异动快报（头条异动 + 异动榜单 + 诚实说明）。
  数据来自币安官方公开资源：官方公开行情接口（data-api.binance.vision 现货全市场快照）、
  官方开源数据仓库（data.binance.vision 合约 K 线与合约指标归档）、
  官方 binance 技能驱动的官方 CLI（binance-cli）；默认通道另接入 Gate.io 与
  Hyperliquid 的公开行情做跨市场交叉观察。
  评分与排序引擎为本仓库自研（app.js 的 scoreMarkets 等函数，命令行与网页共用同一段代码，
  引擎一行未改），结果可复现、不依赖任何大模型。
  当用户问「现在全市场哪些币在异动」「24 小时谁涨得最凶 / 跌得最凶」「成交最活跃的是哪些」
  「把异动榜单拉出来」「某个币为什么出现在榜上」时使用。
metadata:
  version: 1.0.0
  author: ZZKKK123777
  category: market-analysis
  engine: app.js 的 scoreMarkets 等函数（agent.mjs 从源码按字节原样提取复用，引擎未改）
  official_resources:
    - name: data-api.binance.vision
      role: 币安官方公开行情接口（免密钥）。--live 通道用 api/v3/ticker/24hr 不带 symbol 一次拿现货全市场快照
    - name: data.binance.vision
      role: 币安官方开源数据仓库（T+1 归档 ZIP）。--official 通道用合约日 K 线 + 合约指标（持仓量美元值）逐币拼出截至前一交易日的横截面
    - name: binance-cli
      repo: https://github.com/binance/binance-cli
      role: 官方命令行工具。--skill 通道由官方 binance 技能文档记载的 request 子命令取数
  optional_skills:
    - name: binance
      reason: 本技能通过官方 binance 技能文档记载的 binance-cli request 用法取数
  license: MIT
---

# 全市场异动雷达 · 官方技能说明

> 这份技能的产出是一份**异动快报**：先给结论，再给口径。
> 所以这份说明书也按快报的顺序写 —— 先说它管什么、不管什么，再说数据从哪来、
> 分怎么打，最后是「读快报时别过度解读的地方」。

---

## 当班范围（它适合回答什么）

- 「现在全市场哪些币在异动？」→ 快报直接给头条异动 + 异动榜单
- 「24 小时谁涨得最凶 / 跌得最凶？」→ 榜单按综合异动强度排序，方向列区分涨跌
- 「成交最活跃的是哪些？」→ 活跃度分位是评分的组成部分，榜单内可见
- 「帮我按方向筛：只看急跌」→ `--direction fall`
- 「只看某几个币现在什么状态」→ `--coins BTC,ETH,SOL`
- 「这个分是怎么算的？」→ 见下方《0-100 分怎么打出来的》，公式完全公开

## 不在当班范围（它不适合回答什么）

- **不预测价格**。快报只回答「哪里在异动」，不回答「接下来涨还是跌」
- **不回答真实主动成交流向**。公开行情快照里没有逐笔方向数据，
  `方向` 字段是价格涨跌方向口径（详见诚实说明），别当成主动买卖单占比
- **不做单币深度诊断**。单币的 K 线指标、量价背离属于另外的作品，本技能只做横截面
- **不构成投资建议**。评分用于排序观察

---

## 雷达的四个探头（四条数据通道）

同一个引擎（`scoreMarkets`），四个探头，快报抬头会写明本次用的是哪个：

| 探头 | 敲什么 | 数据从哪来 | 口径 | 快报里的通道标识 |
|---|---|---|---|---|
| 多平台交叉（默认） | `node agent.mjs` | Gate.io + Hyperliquid 公开行情（网页版同款数据源） | 实时快照 | `via=multi-platform` |
| 币安实时 | `node agent.mjs --live` | 币安官方公开行情接口，`ticker/24hr` 不带 symbol 一次拿现货全市场 | 实时快照，USDT 计价 + 24h 成交额 > $10,000 过滤 | `via=official-rest` |
| 币安官方 CLI | `node agent.mjs --skill` | 官方 binance 技能驱动的官方 CLI（`binance-cli request GET`） | 同上一份官方公开行情 | `via=binance-cli` |
| 币安官方归档 | `node agent.mjs --official` | 币安官方开源数据仓库：合约日 K 线 + 合约指标（5 分钟粒度持仓量） | **T+1**：截至上一个已收盘交易日的口径，不是此刻实时价 | `via=official-archive` |

设计原则：**探头可以坏，坏要说**。默认通道某个平台取不到数时如实报错，
不会拿演示数据冒充实时；`--skill` 通道 CLI 不可用时回退并在快报里标注
（`via` 字段会变，诚实说明里多一条回退声明）。

---

## 当班记录（2026-09-14 实测，数字随行情变）

> 下面是四条通道各跑一次的真实读数。行情在动，你跑出来的数字一定不同 ——
> 但**格式、字段、通道标识**应该和这里一致。

| 探头 | 监测市场 | 耗时 | 通道标识 | 当时头条 |
|---|---|---|---|---|
| 默认（多平台） | 1365 个 | 0.9 秒 | `via=multi-platform` | — |
| `--live` | 681 个 | 0.9 秒 | `via=official-rest` | LSK −53.59% |
| `--skill` | 681 个 | 1.1 秒 | `via=binance-cli` | LSK −53.59% |
| `--official` | 40 币种拼齐 33 个 | 数秒 | `via=official-archive` | WLD −5.19%（截至 2026-09-13 收盘） |

`--official` 拼不齐的 7 个（MATIC、kPEPE、RNDR、HPOS、RLB、UNIBOT、kSHIB）
是官方归档里没有对应文件（面值币三种前缀都试过），如实跳过并在快报里列出。

---

## 快报里每个字段是什么

| 字段 | 意思 | 怎么来的 |
|---|---|---|
| 头条异动 | 全市场综合异动强度第 1 名 | 引擎排序后的第 1 行 |
| 现价 / 24h | 该币最新价与 24 小时涨跌幅 | 探头原始数据 |
| 异动强度 n/100 | 综合异动评分 | 自研引擎 `scoreMarkets` |
| 高关注 / 中关注 | 80 分以上为高关注，其余中关注 | `score ≥ 80` |
| 活跃度分位 n% | 该币成交额在本批样本里的横截面分位 | 引擎百分位计算 |
| 买盘方向 / 卖盘方向 | **价格方向口径**：涨 = 买盘方向、跌 = 卖盘方向 | 引擎 direction 字段 |
| 监测市场 n 个 | 本批样本总量 | 探头返回并过滤后 |
| 显著异动 n 个 | 60 分以上的币数 | `score ≥ 60` |
| 急涨 / 急跌 | 24h 涨幅 ≥ +5% / ≤ −5% 的币数 | 统计口径 |
| `oiUsd` | 合约持仓量美元值（仅 `--official` 通道有） | 官方合约指标文件 |

`--json` 输出同样的内容，另带 `generatedAt`、`archive`（归档锚定日期）、
`honesty`（完整诚实清单）等字段，便于程序化调用。

---

## 0-100 分怎么打出来的

引擎公式（`app.js` 的 `scoreMarkets`，命令行与网页共用同一段代码，一行未改）：

1. **涨幅分 moveScore** = min(|24h 涨跌幅| × 5.3, 82) —— 变化越剧烈分越高，82 分封顶
2. **活跃度分 activity** = 该币成交额在本批样本里的百分位（0-100）
3. **方向加成 bonus** = |涨跌幅| ≥ 2% 加 8 分，否则加 2 分
4. **综合分** = min(99, round(涨幅分 × 0.62 + 活跃度分 × 0.3 + 方向加成))

举例：某币 24h −53.59%（moveScore 封顶 82），成交额分位 95，方向加成 8
→ min(99, round(82×0.62 + 95×0.3 + 8)) = min(99, 85) ≈ 85 分，显著异动。

---

## 读快报时别过度解读的地方

- **「买盘方向 / 卖盘方向」是价格方向口径**：涨 = 买盘方向、跌 = 卖盘方向。
  公开行情快照无法确认真实主动成交流向，网页版简报里也是同样的声明。
  想要真实主动买卖，需要逐笔成交数据，那是另一个维度的作品。
- **活跃度分位是「样本内」的相对值**：样本不同（通道不同、过滤不同）分位就不同，
  跨通道直接比分位没有意义。
- **`--official` 是 T+1 口径**：价格与涨跌是截至上一交易日收盘的，
  不是此刻实时价，快报里会写明锚定日期。
- **`--official` 的涨跌基准是前一交易日收盘价**：个别新币改用当日开盘价，
  JSON 里 `baseline` 字段会标注。
- **合约持仓量 `oiUsd` 偶有缺失**：官方指标文件个别币种没有覆盖，字段留空，不猜数。
- **评分用于排序观察，不构成投资建议。**
